version: "3.8"

services:
  stock-data-node:
    build: .
    container_name: stock-data-node
    restart: unless-stopped
    environment:
      - APP_BASE_DIR=/app
    volumes:
      # Config files (hot-reload supported)
      - /home/daniel/stock-data-node/config:/app/config
      # Parquet data (readable by dashboard and other services)
      - /home/daniel/stock-data-node/data:/app/data
      # Watch directory (drop .txt files here to trigger downloads)
      - /home/daniel/stock-data-node/watch:/app/watch
      # Log files (accessible from host for inspection)
      - /home/daniel/stock-data-node/logs:/app/logs
      # Persistent state (failed_ticker.json blacklist)
      - /home/daniel/stock-data-node/state:/app/state
    network_mode: "host"
    # Port mappings are ignored when using host networking
    # ports:
    #   - "8000:8002"
